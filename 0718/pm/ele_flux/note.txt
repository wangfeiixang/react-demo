

//因为在多个路由组件里，甚至是路由组件的内部小组件，在获取数据的时候都要使用定位信息

//我们把定位信息放在哪都不合适，如果能放到根组件上，其他子孙组件来用就挺好，
//但是有两个问题  1.根组件用了路由之后，传不进去了
//2.如果组件嵌套的太多，一层一层的传还容器出问题

//所以我们使用了flux，将定位信息放到store里

//在根组件挂载完成之后进行了定位，更改了store里的数据
//在其它组件里直接获取，然后利用某些东西进行数据的更新就可以了